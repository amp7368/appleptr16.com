import {
    SectionHeader,
    SectionHeaderProps,
} from '../../projects/common/SectionHeader';

export function ResumeSectionHeader(props: SectionHeaderProps) {
    return <SectionHeader variant="h2" {...props} />;
}
